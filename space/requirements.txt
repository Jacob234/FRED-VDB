# Runtime deps for the search demo. The fred_search package is vendored into
# this Space (see ./fred_search/), so there's no git dependency.
#
# httpx + numpy are required at IMPORT time: fred_search/__init__.py eagerly
# imports fetch (-> _client -> httpx) and ingest (-> numpy), even though the
# search path never calls them. tqdm/lancedb/sentence-transformers are lazily
# imported inside functions, so only the ones the search path hits are needed.
# gradio 6.x required: gradio 4.x imports the now-removed huggingface_hub.HfFolder,
# which collides with the hf_hub 1.x that sentence-transformers pulls in.
gradio==6.14.0
huggingface_hub>=0.33.5
lancedb>=0.13
sentence-transformers>=3.0
pyarrow>=16.0
pandas>=2.0
httpx>=0.27
numpy>=1.26
